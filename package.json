{
  "name": "my-pandas",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "bootstrap": "lerna bootstrap --hoist",
    "clean": "lerna clean",
    "build": "lerna run build",
    "publish": "lerna publish",
    "start": "cd packages/web && npm start",
    "storybook": "cd packages/web && npm run storybook",
    "test": "lerna run test",
    "json-server": "./node_modules/.bin/json-server --watch ./json-server/db.json --port 3004 --id key"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "devDependencies": {
    "json-server": "0.15.1",
    "lerna": "3.20.2"
  }
}
